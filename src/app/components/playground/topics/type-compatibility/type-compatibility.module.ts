import * as angular from 'angular';
import {IModule} from 'angular';

const TypeCompatibility: IModule = angular
    .module('app.components.playground.topics.type-compatibility', []);

export default TypeCompatibility;