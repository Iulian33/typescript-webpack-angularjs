import * as angular from 'angular';
import {IModule} from 'angular';

const TypeInference: IModule = angular
    .module('app.components.playground.topics.type-inference', []);

export default TypeInference;